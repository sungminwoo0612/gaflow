name: lab-exp-highvis-person

conda_env: ../../conda.yaml

entry_points:
  main:
    command: "python train.py"
    parameters:
      data_path:
        type: str
        default: "datasets/tudorhirtopanu/yolo-highvis-and-person-detection-dataset"
      epochs:
        type: int
        default: 5
      batch_size:
        type: int
        default: 16
      model_size:
        type: str
        default: "yolov8n"
